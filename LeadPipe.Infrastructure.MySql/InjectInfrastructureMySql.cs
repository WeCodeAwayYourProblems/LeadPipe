using Microsoft.Extensions.DependencyInjection;

namespace LeadPipe.Infrastructure.MySql;

public static class InjectInfrastructureMySql
{
    public static IServiceCollection AddInfrastructureMySql(this IServiceCollection services)
    {
        // Format: services.AddScoped<Interface, Class>();

        // Services 

        // Repositories

        // Logging

        // Add databases
        

        return services;
    }
}
